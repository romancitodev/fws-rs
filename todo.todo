fixes:
    ✔ kill processes on reload.

features:
    - add input to execute commands like "rs" to restart the command or "exit" to kill it clean.
    ✔ add CLI arg "--on-events-only" to prevent execution loops. (default false)
    - add CLI arg "--patterns" to watch only files with specific extension. (default None)
